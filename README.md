# source-to-html-c
Built a C program that parses C source files and generates color-coded HTML output for browser display. Implemented token classification (keywords, strings, comments, etc.) using FSM logic to simulate syntax highlighting.
